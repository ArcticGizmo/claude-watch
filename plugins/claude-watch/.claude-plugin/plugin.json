{
  "name": "claude-watch",
  "version": "0.1.0",
  "description": "Integrates Claude Code with the claude-watch tray app: reports each session's live permission mode, and adds /afk (toggle external notifications) and /history (open the history panel). Command logic lives in the tray app (claude-watch handle ...); fails gracefully when claude-watch isn't installed or running.",
  "author": { "name": "Jonathan Howell" },
  "hooks": "./hooks/hooks.json"
}
